name: whiteboard_demo
description: Live whiteboard demo application.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.13.7
  flutter_bloc: ^5.0.1
  uuid: ^2.2.0
  provider: ^4.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.0
  json_serializable: ^3.3.0
  json_annotation: ^3.0.1

flutter:
  uses-material-design: true
